# requirements.txt - Corrected for VPS Deployment
# Core Dependencies
python-telegram-bot==20.7
python-binance==1.0.19
python-dotenv==1.0.0

# Database (sqlite3 is built into Python)
aiosqlite>=0.21.0

# Encryption
cryptography>=41.0.0

# HTTP and async support
requests>=2.31.0
aiohttp>=3.8.0

# Logging and monitoring (optional but recommended)
structlog>=23.0.0
psutil>=5.9.0